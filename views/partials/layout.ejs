<%
  // Access the logged-in user (provided by index.js via res.locals.currentUser)
  const user = typeof currentUser !== 'undefined' ? currentUser : null;
  const isManager = user && (user.role === 'manager' || user.role === 'admin');
%>
<!DOCTYPE html>
<html lang="en">
<head>
  <!-- Head partial (CSS, fonts, <meta>, etc.) -->
  <%- include('_head') %>
</head>

<body>

  <!-- NAVBAR (No variables passed, _nav reads currentUser directly) -->
  <%- include('_nav') %>

  <!-- Main page content from each EJS view -->
  <main class="container">
    <%- body %>
  </main>

  <!-- Footer -->
  <footer class="footer">
    <p>&copy; 2025 Ella Rises. Empowering the rising generation of women.</p>
  </footer>

</body>
</html>